#include<stdio.h>
int main()
{
    // this program will print the first 10 numbers using for loops from 1 to 1o
    //we use the for loop when we know the number of times we have to iterate 
    int i;
    for(i=1;i<=10;i++)
    {
        printf("%d\n",i);
    }
    return 0;
}